# 機能要求定義書: FlexPaste-Solo

## 1. 概要と開発原則

- **プロジェクト名:** FlexPaste-Solo
- **対象プラットフォーム:** Google Chrome 拡張機能 (Manifest V3)
- **目的:** 右クリック（コンテキストメニュー）を起点とし、設定画面で事前作成したカテゴリ別テンプレートと動的変数を組み合わせて、ペーストした瞬間に文脈や時間を即座に反映・完成させる定型文流し込みツール。
- **設計制約 (Soloシリーズ開発ポリシー):**
  - **依存ライブラリゼロ (Vanilla JS):** フレームワーク（React/Vue等）、ビルドツール（Webpack/Vite等）、サードパーティライブラリを一切使用せず、ブラウザ標準技術（ES Modules, DOM API, HTML5, CSS3）のみで構成する。
  - **100% ローカル実行 (プライバシー最優先):** 外部サーバー通信、トラッキング、アナリティクス、リモートAPI呼び出しは一切行わない。すべての処理をユーザーのブラウザ内で完結させる。
  - **デザインシステム:** Native CSS カスタムプロパティを使用し、Material Design 3 (M3) ガイドラインに準拠する。

---

## 2. ディレクトリ構造・ファイル構成

```text
flexpaste-solo/
├── manifest.json
├── background.js       # コンテキストメニュー生成・動的変数解決・ページ注入処理
├── options.html        # 設定画面 (管理エディタ)
├── options.css
├── options.js
├── icons/
│   ├── icon16.png
│   ├── icon48.png
│   └── icon128.png
└── requirements.md
```

### Manifest 仕様 (`manifest.json`)

* **Manifest Version:** 3
* **Permissions:** `contextMenus`, `storage`, `activeTab`, `scripting`
* **Options Page Config:** `options_page` または `options_ui` を `options.html` に設定（拡張機能アイコンクリックおよび右クリック設定メニューから遷移可能）

---

## 3. コンテキストメニュー（右クリック）仕様

### 3.1. 階層構造（カテゴリ別グループ化）

* 右クリックメニュー内に親メニュー `[FlexPaste]` を作成し、その下に設定画面で定義した「カテゴリ」および「定型文テンプレート」を2階層で展開する。
* **構造イメージ:**
```text
[右クリックメニュー]
└── FlexPaste
    ├── 業務連絡 (カテゴリ)
    │   ├── 日報フォーマット
    │   └── 業務終了報告
    ├── 日程調整 (カテゴリ)
    │   └── 会議開催案内
    └── ⚙ 設定 (Options画面を開く)
```

### 3.2. 注入挙動

* コンテキストメニューから目的の定型文をクリックした瞬間、`background.js` が動的変数をリアルタイム解決し、アクティブなタブで現在フォーカスされている入力要素 (`<input>`, `<textarea>`, `[contenteditable]`) へ直接ペースト注入する。

---

## 4. 動的変数エンジン仕様

ペースト実行時（右クリッククリック時）に、テンプレート内のマスタッシュ記法 `{{variable}}` を以下のルールでリアルタイム変換する。

### 4.1. 日時・相対日付変数

1. **基本日時:**
* `{{date_with_day}}` : 例 `2026年8月27日(木)`
* `{{date}}` : 例 `2026/08/27`
* `{{time}}` : 例 `20:41`
* `{{time_with_sec}}` : 例 `20:41:38`

2. **相対日付:**
* `{{yesterday_with_day}}` / `{{yesterday}}` : 昨日（1日前）
* `{{tomorrow_with_day}}` / `{{tomorrow}}` : 明日（1日後）
* `{{next_week_with_day}}` / `{{next_week}}` : 来週の同曜日（7日後）

3. **月末・稼働日計算:**
* `{{month_end}}` : 当月末日の日付（例: `2026/08/31`）
* `{{month_last_workday}}` : 当月最終稼働日の日付。
* **計算ロジック:** 当月末日から逆算し、設定画面で指定された「稼働曜日（デフォルト: 月〜金）」に該当する最新の日付を出力する。

### 4.2. 文脈（コンテキスト）変数

* `{{selection}}` : 右クリックした際にWebページ上で**マウス選択していた文字列**を取り込んで置換する。
* `{{page_title}}` : 現在開いているページのタイトル。
* `{{page_url}}` : 現在開いているページのURL。

---

## 5. 設定画面 (管理エディタ / options.html) 仕様

### 5.1. カテゴリ ＆ テンプレート管理

* **カテゴリ管理:** カテゴリの作成・編集・削除・並び替え。
* **テンプレート編集:**
* 各テンプレートは「タイトル」と「本文（テキストエリア）」で構成される。
* ドラッグ＆ドロップによるテンプレートの自由な並び替え（コンテキストメニューの表示順に同期）。

### 5.2. ノーコード変数挿入 UI

* テンプレート編集テキストエリアの上部に、ドラッグ可能な「変数チップ群」（例: `[ 📅日付(曜日有) ]`, `[ ✂️選択テキスト ]`, `[ 🏢月最終稼働日 ]`）を配置。
* **操作:** 変数チップをテキストエリア内へドラッグ＆ドロップ（またはクリック）すると、カーソル位置に対応するマスタッシュタグ（例: `{{selection}}`）が自動挿入される。

### 5.3. リアルタイムプレビュー

* 編集中のテンプレートの下部に、現在の実時間および仮の選択テキストで評価された**最終ペースト出力のリアルタイムプレビュー**を表示する。

### 5.4. システム設定

* **稼働曜日設定:** 「月・火・水・木・金・土・日」のチェックボックス（デフォルト: 月〜金にチェック）。`{{month_last_workday}}` の計算ロジックに使用。
* **バックアップ・復元:** 作成したカテゴリ・テンプレート一式を JSON ファイルとしてエクスポート / インポートする機能。

---

## 6. 技術実装指示

1. **Context Menu Dynamic Generation:** `chrome.storage.local` に保存されたテンプレートデータ構造に基づき、`chrome.contextMenus.create` を使用して起動時および設定変更時にコンテキストメニューを動的に再構築する。
2. **Text Injection:** アクティブタブのフォーカス要素に対し、`chrome.scripting.executeScript` を用いてテキストを直接代入し、`input` / `change` イベントを発火させてモダンWebアプリケーション（React/Vue等）に正常認識させる。
3. **Storage Schema:**
```json
{
  "settings": {
    "workdays": [1, 2, 3, 4, 5] // 1: Mon, 5: Fri
  },
  "categories": [
    {
      "id": "cat_1",
      "title": "業務連絡",
      "templates": [
        {
          "id": "tpl_1",
          "title": "業務終了報告",
          "content": "本日の業務を終了します。\n稼働時間: 9:00-{{time}}\n対象: {{selection}}"
        }
      ]
    }
  ]
}
```
